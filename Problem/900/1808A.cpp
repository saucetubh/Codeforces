#include <iostream>
using namespace std;

void solve() {
    int l, r;
    cin >> l >> r;
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}